import { icon } from 'leaflet';
import inconMap from '../assets/iconMap.svg';

export const IconLocationComponent = icon({
    iconUrl: inconMap,
    iconAnchor: null,
    shadowUrl: null,
    shadowSize: null,
    iconSize: [35, 35],
    className: 'Leaftleft-venue-icon',
});
